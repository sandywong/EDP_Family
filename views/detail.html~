 <div class="content">
     <p>
        <table>
            <tr><td>花名:</td><td><% var nickName = person.nickName == null ? '无' : person.nickName %><%= nickName %></td></tr>
            <tr><td>姓名:</td><td><% var name = person.name == null ? '无' : person.name %><%= name %></td></tr>
            <tr><td>分机号:</td><td><% var extenNum = person.extenNum == null ? '无' : person.extenNum %><%= extenNum %></td></tr>
            <tr><td>旺旺:</td><td><% var wangWang = person.wangWang == null ? '无' : person.wangWang %><%= wangWang %></td></tr>
            <tr><td>手机:</td><td><%= person.cellPhone %></td></tr>
            <tr><td>邮箱:</td><td><% var mail = person.mail == null ? '无' : person.mail %><%- mail %></td></tr>
            <tr><td>微博:</td><td><% var weibo = person.weibo == null ? '无' : person.weibo %><%- weibo %></td></tr>
            <tr><td>职位:</td><td><% var position = person.position == null ? '无' : person.position %><%- position %></td></tr>
            <tr><td>座位:</td><td><% var place = person.place == null ? '无' : person.place %><%- place %></td></tr>
        </table>
     </p>
     <p>
     <a href="javascript: history.back();">返回<a>
     <a href="/">首页</a>
     </p>
 </div>
